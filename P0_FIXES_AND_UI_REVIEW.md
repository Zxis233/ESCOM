# ESCOM P0 修复与 UI 检查记录

- 检查日期：2026-08-01
- 基线提交：`f7f65eb`
- 检查范围：接收缓存、显示格式化、搜索索引、egui 接收区与设置窗口

## 结论

三个 P0 问题均已修复。持续接收数据时，常规路径不再反复格式化和搜索整个缓存；搜索输入的防抖计时不会被接收刷新重置；搜索索引只有在与当前显示代次一致时才允许用于过滤、高亮和导航。

正常接收更新的主要成本从“全缓存 O(N)”降为“新增数据和受影响行 O(Δ)”。初始加载、显示模式或编码变化、缓存游标断档等低频场景仍会执行一次完整重建。

## 原有 P0 问题

### P0-1：持续接收时搜索可能永远无法开始

原逻辑每次后台格式化完成都会调用搜索请求，并重置 `120 ms` 搜索防抖时间。格式化刷新间隔约为 `80 ms`，因此串口持续有数据时，搜索计时可能一直达不到触发阈值。

影响：搜索栏长期显示“搜索中”，过滤、上一处和下一处无法稳定得到结果。

### P0-2：新显示数据可能短暂使用旧搜索索引

显示行更新后到新搜索结束前，旧 `SearchIndex` 仍可能被用于：

- 过滤行；
- 计算匹配高亮字节范围；
- 上一处/下一处导航；
- 计算滚动目标。

旧索引中的行号和字节范围属于上一版 `display_rows`，应用到新行后可能高亮错误文本、过滤错误行，甚至产生越界风险。

### P0-3：每次接收刷新都全量格式化并全量搜索

原路径在每个接收代次上复制完整快照、格式化全部缓存，再对全部显示行执行搜索。缓存上限允许达到 `500 MiB`，持续接收时会形成重复 O(N) 扫描、字符串分配和后台线程开销。

影响：缓存越大，CPU、内存峰值和 UI 延迟越明显。

## 已实施修复

### 1. 增量接收游标

`src/store.rs` 新增 `ReceiveCursor`、`ReceiveDelta` 和 `ReceiveStore::delta_since`：

- 只返回游标之后的新接收块；
- 返回当前最早保留序号，供显示层淘汰旧行；
- 使用 `stream_id` 识别清空操作；
- 当未处理数据已被缓存淘汰时报告断档，转入完整重建。

### 2. 流式增量格式化

`src/formatting.rs` 新增 `DisplayFormatter`：

- UTF-8/GBK 解码器跨接收块保留状态；
- CR、LF、CRLF 可跨块正确组合；
- 只追加新完成的行；
- 未换行的末行在新数据到来时只替换尾行；
- 根据接收块序号移除已淘汰的完整行；
- HEX 模式只格式化新增块。

应用层只在首次显示、模式/编码切换、清空后重建或游标断档时启动完整后台格式化。普通接收刷新直接应用小型 `DisplayUpdate`。

### 3. 搜索防抖与接收刷新解耦

只有用户修改搜索文本、大小写、正则模式或时间戳显示时，才会重置搜索防抖时间。接收数据更新不会再改写 `search_last_changed`。

用户输入防抖期间，显示仍可增量更新；到达 `120 ms` 后，搜索会针对当时最新的显示行启动。

### 4. 搜索索引绑定显示代次

后台搜索事件现在携带 `display_generation`。结果只有在搜索令牌和显示代次同时匹配时才会被接受。

UI 使用搜索索引前统一检查其代次：

- 代次不一致时不启用过滤；
- 不绘制旧匹配高亮；
- 禁用上一处/下一处；
- 不使用旧索引计算滚动目标；
- 状态栏显示“搜索中…”。

完整搜索运行期间暂停显示格式化更新，使被搜索的 `Arc<Vec<FormattedRow>>` 保持稳定。串口接收和原始缓存写入仍继续；搜索结束后再增量追赶显示。

### 5. 搜索索引增量维护

`src/search.rs` 新增可复用的 `SearchMatcher`，并为 `SearchIndex` 增加增量更新：

- 缓存头部淘汰后平移剩余行号；
- 未换行尾行被替换时删除旧匹配并重扫该尾行；
- 只扫描新增行；
- 保持匹配行和匹配范围有序。

因此查询不变时，接收刷新不会再触发全缓存搜索。

## 验证结果

### 自动验证

- `cargo test --all-targets`：55 项通过，0 项失败；
- `cargo clippy --all-targets -- -D warnings`：通过；
- `cargo build`：通过；
- `git diff --check`：无空白错误。

新增覆盖包括：

- 增量游标只返回新块；
- 清空和缓存断档检测；
- 分块 UTF-8、GBK、CRLF 解码；
- 未换行尾行替换；
- 缓存淘汰后的行移除；
- 无新数据时不重复追加尾行；
- 搜索索引头部平移、尾行替换和新增行扫描。

### UI 实际检查

已构建并启动 Windows 桌面程序，检查以下状态：

- `1280 × 820` 默认窗口：连接区、接收工具栏、搜索栏、接收区和状态栏无重叠；
- `1024 × 640` 最小窗口：主工具栏仍完整显示，搜索输入框可用；
- 字体与显示设置页：标签、组合框、滑块和缓存选项对齐正常；
- 应用背景设置页：卡片和滑块无重叠，最小窗口下通过垂直滚动访问下方预览；
- 空数据搜索：正确显示 `0/0`，导航按钮禁用；
- 非法 ASCII 正则：正确显示红色“表达式错误”，过滤和导航禁用。

`computer-use` 原生控制管道在本机返回“系统找不到指定的文件”，因此实际 UI 检查使用 Win32 窗口定位、本地点击和 DPI 感知截图完成。测试进程已关闭，窗口恢复为 `1280 × 820`。

## 仍可优化的 UI 项

这些问题不属于本次三个 P0，未在本次修改中调整：

### P2：允许 48 pt 界面字号，但多个控件使用固定高度

界面字号允许 `10–48 pt`，但标题栏、工具栏和输入控件主要使用 `32–38 px` 固定高度。大字号下可能出现文字裁切、控件拥挤和搜索栏横向溢出。

建议：将界面字号上限与控件高度联动，或把界面字号上限收紧到经过布局验证的范围；数据字号可以继续保留较大范围。

### P2：设置窗口固定宽度且默认位置偏左上

设置窗口固定为约 `660 px`，在最小窗口下会覆盖左侧大部分主界面，右侧保留较大空白。背景预览位于首屏以下。

建议：默认居中；宽度使用视口约束；在背景页压缩说明文字和垂直间距，使预览更早出现。

### P2：接收行高使用经验公式

接收区行高为 `data_font_size + 7 px`，没有基于实际字体字形度量。部分中文字体、可变字体或高字号可能出现上下裁切或行距不一致。

建议：从字体排版结果获取实际行高，再叠加用户行距。

### P3：搜索栏在最小宽度下接近容量上限

当前 `1024 px` 最小宽度可以完整显示，但搜索栏右侧包含清除、大小写、正则、过滤、导航、状态和高亮菜单，界面字号增大或文案变长后容易拥挤。

建议：将上一处/下一处改为带工具提示的图标按钮，状态区域设置稳定最小宽度，并在空间不足时把高亮菜单移到下一行或溢出菜单。

## 正确性与性能边界

### 会执行完整格式化的场景

- 应用首次加载；
- 接收模式或文本编码变化；
- 暂停后恢复显示；
- 清空接收区；
- 缓存限制变化触发强制重建；
- 显示游标落后过多，未处理接收块已被淘汰。

这些是低频 O(N) 操作，避免了持续接收时的重复 O(N)。

### 搜索查询变化仍需完整扫描

搜索文本、大小写、正则模式或时间戳显示变化后，需要对当前显示行完整搜索一次。搜索期间显示会暂时停在一致的代次，原始串口接收继续进行。

### 未换行超长文本仍是特殊热点

增量格式化只替换最后一行，但如果设备长期不发送换行符，最后一行本身可能非常大。每次更新仍需复制和重新搜索该行，成本为 O(当前行长度)。这是目前最主要的剩余性能边界。

### 缓存淘汰与跨块文本行

完整行按“结束该行的接收块序号”淘汰。若一行从已淘汰块开始、在保留块中仍未结束，为保持流式解码连续性，该活动行可能暂时保留已淘汰前缀，直到换行或完整重建。

### 不完整多字节字符

显示格式化不会在每次增量刷新时强制结束解码器，因此尾部不完整 UTF-8/GBK 字符会等待后续字节，不会提前显示替换字符。导出仍使用完整快照并结束解码，末尾非法或不完整字节的表现可能与实时显示不同。

### 搜索匹配上限

搜索最多记录 `100,000` 个匹配。达到上限后 `truncated = true`；后续缓存头部淘汰会保持已有索引行号正确，但不会回填先前因上限而省略的匹配。过滤结果因此仍受匹配上限约束。

### 未覆盖环境

- 未连接真实串口硬件，未做持续满速串口的端到端 UI 压力测试；
- 未验证屏幕阅读器；
- 未逐项验证所有 Windows DPI 缩放比例和 48 pt 界面字号；
- `u64` 接收序号回绕属于理论边界，当前比较逻辑未为实际回绕设计。

## 主要代码位置

- `src/store.rs`：接收游标、增量数据和断档检测；
- `src/formatting.rs`：流式增量格式化和显示更新；
- `src/search.rs`：可复用匹配器和增量搜索索引；
- `src/app.rs`：任务调度、显示代次校验、UI 索引保护。
